# Frontend for the assignments
## Getting Started
Prerequisites: yarn

First, install dependencies

`yarn install`

Then spin up a dev server with 

`yarn dev`

Dependencies:
- React
- MantineUI
- Tanstack Query (for query management)